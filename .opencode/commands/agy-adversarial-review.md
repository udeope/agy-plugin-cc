---
description: Ask agy to run an adversarial review of current git changes.
argument-hint: "[--background] [--base <ref>] [extra instructions]"
---

Run this command from the current workspace and return stdout/stderr verbatim:

```bash
agy-companion adversarial-review "$ARGUMENTS"
```

Do not inspect files yourself and do not summarize the output.
