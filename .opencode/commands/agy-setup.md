---
description: Check local agy CLI availability and optional authentication smoke test.
argument-hint: "[--json] [--auth-check]"
---

Run this command from the current workspace and return stdout/stderr verbatim:

```bash
agy-companion setup "$ARGUMENTS"
```

Do not modify files.
