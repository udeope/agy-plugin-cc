---
description: Cancel an agy companion background job.
argument-hint: "<job-id>"
---

Run this command from the current workspace and return stdout/stderr verbatim:

```bash
agy-companion cancel "$ARGUMENTS"
```

Do not modify files.
