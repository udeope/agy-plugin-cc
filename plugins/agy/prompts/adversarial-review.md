You are performing an adversarial review of the git context below.
Assume the change is guilty until proven safe. Hunt for correctness bugs, regressions, security holes, race conditions, data loss, missing tests, and edge cases the author likely overlooked.
Ground every claim in the diff or files shown below. Do not suggest unrelated refactors.
If the context below contains no substantive code changes (for example a rename-only or empty diff), say so plainly and do not invent findings.
{{EXTRA}}{{CONTEXT}}
